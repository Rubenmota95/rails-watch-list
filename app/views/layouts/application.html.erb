<!DOCTYPE html>
<html>
  <head>
    <title>RailsWatchList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <div class="d-flex justify-content-between pt-4 pb-4 bg-light">
      <i class="fa-regular fa-file-video display-5 "></i>
      <div class="d-flex justify-content-end ">
        <%= link_to "My Lists", lists_path, class: "text-decoration-none text-secondary" %>
        <p class="mx-3"> | </p>
        <%= link_to "Add List", new_list_path, class: "text-decoration-none text-secondary" %>
      </div>
    </div>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
